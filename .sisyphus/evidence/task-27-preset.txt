Task 27: Default Preset Loading - Evidence

Date: 2026-04-21

Changes made:
- Modified src/App.tsx to add useEffect for initial track generation
- Added imports: useEffect from react, useAppStore, generateTrack, defaultConfig
- Added useEffect hook that checks if currentTrack is null and generates a track using defaultConfig

Verification:
- npm run build: PASSED (tsc + vite build succeeded)
- LSP diagnostics: Clean (no errors)

Behavior:
- On first app load, currentTrack is null in the store
- useEffect detects null currentTrack and calls generateTrack(defaultConfig)
- Generated track is set via setTrack(track)
- Default gate counts from defaultConfig are applied: standard=5, h-gate=2, huerdel=1, doppelgate=1, ladder=0, start-finish=1, flag=2
- Subsequent renders with existing track do not regenerate (guard: if (!currentTrack))
