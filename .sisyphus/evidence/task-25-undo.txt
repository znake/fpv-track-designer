Task 25: Undo/Redo Controls - COMPLETED

Files created:
- src/components/ui/UndoRedo.tsx

Files modified:
- src/App.tsx (imported and integrated UndoRedo component, replaced placeholder)
- src/store/trackSlice.ts (fixed pre-existing type error: setTrack now accepts Track | null)

Verification:
- Build passes: npm run build succeeds
- Component uses useAppStore for undo, redo, past, future
- Keyboard shortcuts: Ctrl+Z / Cmd+Z for undo, Ctrl+Y / Cmd+Y / Cmd+Shift+Z for redo
- Buttons disabled when past.length === 0 (undo) or future.length === 0 (redo)
- Shows step count: "{past.length} undo steps | {future.length} redo steps"
- Uses Tailwind CSS matching existing design system (gray-700/600/800 colors, text-sm, rounded, space-y-3)
