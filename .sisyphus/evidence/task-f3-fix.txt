Task: Fix GateAdjustment.tsx to always show heading

Changes made:
- Removed early return when no gate selected (lines 21-27 of original)
- Removed early return when gate not found (line 30 of original)
- Changed `currentTrack.gates.find` to `currentTrack?.gates.find` for null safety
- Heading "Gate Adjustment" is now always rendered outside conditional logic
- Close button (✕) only shown when gate is selected (conditional render)
- When no gate selected: shows heading + placeholder message
- When gate selected: shows heading + gate info + rotation/position controls + close button

Build: PASS (npm run build succeeds)
LSP diagnostics: CLEAN (no errors)