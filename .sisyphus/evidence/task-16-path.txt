Task 16: Flight Path Renderer (Arrows) - COMPLETE

File created: src/components/scene/FlightPath.tsx

Verification:
- npm run build: SUCCESS (tsc + vite build passed)
- FlightPath component renders line connecting all gates in order
- Arrow cones along path showing direction (yellow cones)
- Distinct cyan (#00ffff) color for line visibility
- Uses calculateFlightPath from ../../utils/flightPath
- Uses Gate type from ../../types
- Uses @react-three/fiber primitives (line, mesh, group)
